# Server Setup
PORT=5000
JWT_SECRET=art_website_secret_2026_key

# Cloudinary Integration (Optional: falls back to local disk uploads if empty/unconfigured)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Google Authentication Integration (Optional: falls check to custom login/signup if empty/unconfigured)
GOOGLE_CLIENT_ID=
